# spec-website (Contract-Driven)

This skill now reads contracts/<kit-name>/contract.yaml to determine what specification documents to generate.

The contract's `required_inputs` defines what spec files must exist.
The contract's `acceptance` criteria become the spec's acceptance criteria.
