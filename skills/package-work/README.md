# package-work (Contract-Driven)

This skill now reads contracts/<kit-name>/contract.yaml to determine the structure of every work package.

No hardcoded structures. Every work package is 100% derived from the contract.
This means adding a new Capability Kit requires only writing a contract, not modifying this skill.
