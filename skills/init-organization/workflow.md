# Workflow â€” init-organization

1. **Identify organization** â€” get name from user or prompt for it
2. **Discovery dialog** â€” ask the 8 discovery questions one at a time
3. **Create directories** â€” build the full directory tree
4. **Write constitution** â€” create constitution.md v0.1
5. **Write state files** â€” create all files in state/, memory/, knowledge/
6. **Set active** â€” write status.json with state = "active"
7. **Confirm** â€” display organization name, path, and next steps
